#include<bits/stdc++.h>
using namespace std;

int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    /// code

    long long a, b, x;

    ///cin >> a >> b;
    scanf("%lld%lld", &a, &b);
    x = a + b;
    ///cout << "X = " << x << endl;
    printf("X = %lld\n", x);

    return 0;
}
