#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    /// Your code here
    long long a, b;

    scanf("%lld%lld", &a, &b);
    /// cin >> a >> b;

    printf("PROD = %lld\n", a*b);
    /// cout << "PROD = " << a*b << "\n";

    return 0;
}
