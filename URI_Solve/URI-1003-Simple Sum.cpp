#include<bits/stdc++.h>
using namespace std;

int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    long long a, b, sum;

    scanf("%lld%lld", &a, &b);
    /// cin >> a >> b;

    sum = a + b;

    printf("SOMA = %lld\n", sum);
    /// cout << "SOMA = " << sum << "\n";

    return 0;
}
